name: Nodriver Automation
on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-nodriver:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          # Adding dbus-x11 to provide the 'bus' Chrome is looking for
          sudo apt-get install -y google-chrome-stable xvfb libgbm1 libasound2t64 dbus-x11

      - name: Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Launch Chrome and Run Script
        run: |
          # 1. Start a private D-Bus session to stop those bus errors
          export $(dbus-launch)
          
          # 2. Start Xvfb
          Xvfb :99 -screen 0 1920x1080x24 &
          export DISPLAY=:99
          
          # 3. Launch Chrome with every possible bypass flag
          google-chrome \
            --remote-debugging-port=9222 \
            --remote-allow-origins=* \
            --no-sandbox \
            --disable-setuid-sandbox \
            --disable-dev-shm-usage \
            --disable-gpu \
            --no-first-run \
            --no-zygote \
            --headless=new \
            --user-data-dir=/tmp/chrome-profile &
          
          echo "Waiting for Chrome to initialize..."
          sleep 15
          
          # 4. Run script
          python test_script.py
          
          # 5. Cleanup
          pkill google-chrome

      - name: Upload Result
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nodriver-result
          path: nodriver_final.png
